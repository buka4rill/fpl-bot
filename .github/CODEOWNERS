# Every path defaults to the repo owner. Not an enforcement mechanism —
# require_code_owner_reviews on main is deliberately off (any 1 approval
# satisfies the PR review requirement now, see CLAUDE.md's "Deploy
# (Fly.io) + CI/CD" section) — this just makes GitHub auto-request
# buka4rill as a reviewer on every new PR. The actual "only I can merge"
# guarantee is collaborator permissions (buka4rill is the sole
# collaborator with write access), not a branch-protection push
# restriction — that GitHub feature only exists for organization-owned
# repos, and this one is a personal-account repo.
* @buka4rill
