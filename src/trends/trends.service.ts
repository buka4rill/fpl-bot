import { Injectable } from '@nestjs/common';

@Injectable()
export class TrendsService {
  // TODO: pull from TREND_SOURCES and produce a template-team / differential
  // signal to feed PredictionModule as a secondary input.
}
